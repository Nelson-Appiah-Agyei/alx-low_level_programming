#include "main.h"
#include <stdio.h>

/**
 * main - We are in argc and argv 
 * @argc: it is called the argument count 
 * @argv: it is named as the argument vector
 * Return: Always 0;
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
